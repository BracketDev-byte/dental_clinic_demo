// Always derive types from either table or queried data to avoid data shape mismatches and ensure type safety. This is especially important when working with databases, as the structure of the data can change over time. By deriving types from the actual data, you can catch potential issues at compile time rather than at runtime.
// DO NOT USE 'any" TYPE!!!
// Export types from here, so it is organized.
